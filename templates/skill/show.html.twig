{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ skill.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ skill.description }}</td>
            </tr>
            <tr>
                <th>Accreditation</th>
                <td>{{ skill.accreditation }}</td>
            </tr>
            <tr>
                <th>Drawing</th>
                <td>{{ skill.drawing }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('skill_index') }}">back to list</a>

    <a href="{{ path('skill_edit', {'id': skill.id}) }}">edit</a>

    {{ include('skill/_delete_form.html.twig') }}
{% endblock %}
